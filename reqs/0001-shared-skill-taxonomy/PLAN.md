# PLAN.md — 当前执行计划（需求：0001）

- **依据需求基线版本**: v1
- **计划状态**: 当前
- **最近更新**: 2026-09-08

## 目标、架构与环境约束

将技能目录按能力域嵌套在 `shared/` 下；每个技能仍保持自身的 `SKILL.md`、脚本和参考资料相对路径。所有 `/home/02-skill-fc/skills` 的目录变动、配置和测试均在 WSL 中执行。既有未提交的 `ai-control-plane`、`splc-flash-loop` 与 `wsl-safe-writing` 内容不得修改或暂存。

## 执行步骤

- [x] 运行目标路径前置检查；确认其因新分类目录不存在而失败。
- [x] 用 `git mv` 将 8 个目标技能迁入四个 `shared/` 能力域目录；确认旧 `codex/` 重复目录消失且资源完整。
- [x] 修改 Codex `config.toml` 的受影响路径，保留 `splc-flash-loop`、`splc-sync-feature` 和 `wsl-safe-writing` 的现有配置语义。
- [x] 更新 Kilo、DSH 安装脚本为递归发现共享技能；保持已有同名目标不覆盖的行为。
- [x] 更新 README 的目录树、共享技能分类和放置规范。
- [x] 运行目标路径、配置引用、所有移动后技能校验、安装脚本语法检查、UTF-8 和 `git diff --check`；确认无关工作树改动仍未暂存。

## 依赖、风险与回滚

- 依赖：Codex 配置继续接受指向技能目录的 WSL UNC 路径。
- 风险：嵌套目录会使仅遍历 `shared/*/` 的安装脚本漏装技能；通过递归发现修复。
- 回滚：未提交前可用 Git 将本次目录移动和文档/脚本更改恢复到当前 `HEAD`；不触及其他未提交文件。
