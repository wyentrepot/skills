# PLAN.md — 当前执行计划（需求：<0001>）

> 本文件是当前需求唯一的细粒度执行计划，由 `writing-plans` 生成和更新。
> 它只对应当前生效的需求基线；需求变更影响实现时，先标记本计划失效，再重新调用 `writing-plans`。需求历史仍以 `REQS.md` 为准。

- **依据需求基线版本**: <v2>
- **计划状态**: 当前 / 失效（基线 <旧版本>）
- **最近更新**: YYYY-MM-DD

## 目标、架构与环境约束

<由 writing-plans 根据 REQS.md 填写>

## 执行步骤

- [ ] <文件路径、职责、前置失败检查、最小改动和验证命令>

## 依赖、风险与回滚

<由 writing-plans 根据任务风险填写>
