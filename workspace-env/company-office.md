# 🏢 公司办公环境 — WSL + Windows 双环境

> 环境类型：**同一台机器同时使用 WSL（Linux）与 Windows 两套环境**。

## 概览

- **操作系统**：Windows（宿主）+ WSL（Linux）双环境
- **特点**：跨环境协同，涉及 Linux 侧开发与 Windows 硬件侧操作

## Linux（WSL）侧

| 项 | 值 / 说明 |
|----|-----------|
| WSL 发行版 | <!-- 待填，如 Ubuntu 22.04 --> |
| 工作根目录（WSL） | <!-- 待填，如 ~/projects --> |
| 默认 Shell | <!-- 待填 --> |
| 语言/工具链 | <!-- 待填（python / node / go 等与版本） --> |

## Windows 侧

| 项 | 值 / 说明 |
|----|-----------|
| 工作根目录（Windows） | <!-- 待填，如 D:\work --> |
| 硬件 / 外设 | <!-- 待填（COM 口、烧录器、固件路径等） --> |
| 工具链 | <!-- 待填 --> |

## 跨环境要点

- 路径映射 `\\wsl$` 与 Windows 盘符如何互访（如 `D:\` ↔ `/mnt/d/`）
- 哪些工作放 WSL，哪些放 Windows
- 网络代理 / 端口差异（如有）

## 加载方式

> ⚠️ **待定** —— 见 [README.md](./README.md#加载方式)。