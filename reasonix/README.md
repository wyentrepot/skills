# Reasonix Skills

此目录存放仅 Reasonix（Windows 环境）使用的技能。

每个技能是一个子目录，内含 `SKILL.md`。

加载方式：在 `~/.reasonix/config.toml` 中配置 `[skills] paths = ["<仓库根>/shared", "<仓库根>/reasonix"]`

当前技能：11 个 `superpowers-*`（软件工程方法论，移植自 obra/superpowers，MIT 许可），详见仓库根 README。
